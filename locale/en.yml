fof-oauth:
  admin:
    settings:
      providers:
        apple:
          description: Register your forum with Apple {link}

          client_id_label: => fof-oauth.ref.settings.client_id
          key_file_id_label: Key File ID
          keyfile_upload_label: Key File
          keyfile_upload_help: Create a <a>keyfile</a> and upload it here
          keyfile_upload:
            upload_button: Upload keyfile
            remove_button: Remove keyfile
          team_id_label: Team ID

  forum:
    log_in:
      with_apple_button: Sign in with {provider}

    providers:
      apple: "=> fof-oauth.lib.providers.apple"

  lib:
    providers:
      apple: Apple
